# npm 10.9.x crashes resolving Nuxt's peer graph
# ("Cannot read properties of null (reading 'edgesOut')" in arborist).
# Pinning this here keeps `npm install` and CI's `npm ci` on the same
# resolution strategy instead of relying on a remembered CLI flag.
legacy-peer-deps=true
